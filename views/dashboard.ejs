<% include ../views/layouts/header %>
<% include ../views/layouts/menu %>
                    </div>
                    <!-- /#left -->
                <div id="content">
                    <div class="outer">
                        <div class="inner bg-light lter">

                        <% include ../views/layouts/messages %>
<hr>
<div class="row">
    <div class="col-lg-12">
        <p class="col-lg-12">You have <%= projectsOwned %> project(s)! <a href="projects/new" class="btn btn-primary">Create New Project</a></p>
            <% for (var i=0; i < projects.length; i++) { %>
                <% if (projects[i].owner.username == user.username) { %>
                <div class="col-lg-12">
                    <div class="box">
                        <div class="body">
                            <strong><%= projects[i].name %></strong>
                	    </div>
                        <div class="body">
                            <%= projects[i].description %>
                        </div>
                        <div class="body">
                            Created: <%= projects[i].created.date %> <%= projects[i].created.time %>
                        </div>
                        <div class="body">
                            Tasks: <%= projects[i].tasks.length %>
                        </div>
                        <div class="body">
                            Users: <%= projects[i].users.length %>
                        </div>
                        <div class="body">
                            Task Types: <%= projects[i].tasktypes.length %>
                        </div>
                        <div class="body text-right">
                            <form style="float: right; margin-right: 20px;" method="post" action="/projects/moveProjectToTrash/<%= projects[i]._id %>"><button type="submit" href="/dashboard" class="btn btn-danger">Delete</button></form>
                            <!-- <a href="projects/view/<%= projects[i]._id %>" class="btn btn-primary"><i class="fa fa-users"></i></a>
                            <a href="projects/view/<%= projects[i]._id %>" class="btn btn-warning"><i class="fa fa-gears"></i></a> -->
                            <a style="float: right; margin-right: 20px;" href="/projects/edit/<%= projects[i]._id %>" class="btn btn-warning"><i class="fa fa-pencil"></i></a>
                            <a style="margin-right: 25px;" href="/projects/view/<%= projects[i]._id %>" class="btn btn-success">View</a>
                	    </div>
                	</div>
                </div>
            <% } %>
        <% } %>
        </div>
    </div>
<hr>
<div class="row">
    <div class="col-lg-12">
        <div class="col-lg-12">
            <p class="col-lg-12">You have <%= projectsShared %> shared project(s)!</p>
                <% for (var i=0; i < projects.length; i++) { %>
                <% if (projects[i].owner.username != user.username) { %>
                    <div class="col-lg-2">
                        <div class="box">
                            <div class="body">
                                <strong><%= projects[i].name %></strong>
                            </div>
                            <div class="body">
                                <%= projects[i].description %>
                            </div>
                            <div class="body">
                                Created: <%= projects[i].created.date %> <%= projects[i].created.time %>
                            </div>
                            <div class="body">
                                Tasks: <%= projects[i].tasks.length %>
                            </div>
                            <div class="body">
                                Users: <%= projects[i].users.length %>
                            </div>
                            <div class="body">
                                Task Types: <%= projects[i].tasktypes.length %>
                            </div>
                            <div class="body text-right">
                                <a href="/projects/view/<%= projects[i]._id %>" class="btn btn-success">View</a>
                                <!-- <a href="projects/view/<%= projects[i]._id %>" class="btn btn-primary"><i class="fa fa-users"></i></a>
                                <a href="projects/view/<%= projects[i]._id %>" class="btn btn-warning"><i class="fa fa-gears"></i></a> -->
                                <a href="/projects/edit/<%= projects[i]._id %>" class="btn btn-warning"><i class="fa fa-pencil"></i></a>
                                <a href="/projects/view/<%= projects[i]._id %>" class="btn btn-danger"><i class="fa fa-trash-o"></i></a>
                            </div>
                        </div>
                    </div>
                <% } %>
            <% } %>
            </div>
        </div>
</div>

                        </div>
                        <!-- /.inner -->
                    </div>
                    <!-- /.outer -->
                </div>
                <!-- /#content -->

            </div>
            <!-- /#wrap -->
<% include ../views/layouts/footer %>
